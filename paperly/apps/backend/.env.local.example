# Local Environment Override Example
# Copy this to .env.local for local-specific settings

# Platform-specific overrides
# Uncomment and set if auto-detection doesn't work

# For manual host configuration:
# DB_HOST_OVERRIDE=192.168.1.100
# REDIS_HOST_OVERRIDE=192.168.1.100

# For specific environments:
# WSL with Docker Desktop: Usually auto-detected
# macOS with Docker Desktop: Usually auto-detected
# Linux with Docker: localhost

# Other local overrides
# PORT=3001
# LOG_LEVEL=debug